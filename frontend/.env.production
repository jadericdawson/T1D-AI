# Production environment - uses relative path for same-domain API
# When building for Azure, VITE_API_URL is NOT set, so it defaults to empty
# (same domain, relative API paths). For local dev, .env.local can override.
# VITE_API_URL is intentionally NOT set here
